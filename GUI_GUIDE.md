# CDQC Workbench — 폴더 기반 진단 GUI

이 GUI는 사내 PC의 Python이 파일을 직접 읽고 연산하며, localhost 브라우저에서 결과를 확인하는 도구다. 개별 파일을 업로드할 필요 없이 **루트 폴더와 매칭 규칙을 저장해 여러 측정 건을 일괄 처리**한다. 기존 `cdqc` 연산 라이브러리와 GUI·파일 처리는 분리되어 있다.

## 1. 실행

Windows / Python 3.13 기준으로 저장소 폴더에서 실행한다.

```powershell
git pull
# 처음 설치할 때만 가상환경 생성. 이미 있으면 생략한다.
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements-gui.txt
.\start_gui.ps1
```

`start_gui.cmd`를 더블클릭해도 된다. 기본 주소는 `http://127.0.0.1:8501`이다. 열린 실행 창은 유지하고, 종료할 때 그 창에서 `Ctrl+C`를 누른다. 브라우저가 자동으로 열리지 않으면 주소를 직접 입력한다.

```powershell
# 다른 Python 환경 또는 포트가 필요할 때
.\start_gui.ps1 -Python python -Port 8502
# 브라우저 자동 실행 없이 서버만 시작
.\start_gui.ps1 -NoBrowser
# Python 모듈로 시작할 수도 있다.
.\.venv\Scripts\python.exe -m cdqc_workbench
```

기존 Python 환경에 GUI 의존성이 없다면 첫 업데이트 때 설치가 필요하다. 라이브러리만 사용할 때는 기존 `pip install -e .` 흐름을 계속 사용할 수 있다.

### 인터넷이 없는 PC에 설치

외부 PC에서 대상과 동일한 Python 3.13 / Windows 64-bit용 패키지를 준비한다.

```powershell
.\scripts\fetch_wheels.ps1 -IncludeGui
```

준비한 `wheels` 폴더를 허용된 반입 절차로 가져온 뒤 저장소에서 실행한다.

```powershell
.\.venv\Scripts\python.exe -m pip install --no-index --find-links wheels -r requirements-gui.txt
.\start_gui.ps1
```

패키지 파일은 Git에 포함하지 않는다. 실행 중 파일 읽기·연산·화면 표시는 로컬에서 처리한다. 실행 설정은 서버를 `127.0.0.1`에 바인딩하고 Streamlit 사용 통계 전송을 끈다. 이 설정은 [Streamlit 설정 문서](https://docs.streamlit.io/develop/api-reference/configuration/config.toml)를 기준으로 한다.

## 2. 폴더와 파일 읽기 규칙

먼저 데모 분석으로 화면이 정상적으로 동작하는지 확인할 수 있다. 실제 자료는 루트 폴더를 지정하고 파일 매칭 규칙을 설정한다. 하위 폴더에 분산된 파일도 탐색한다.

설정에서 다룰 내용은 다음과 같다.

| 설정 | 의미 |
|---|---|
| 루트 폴더 | 탐색을 시작할 폴더 |
| 재귀 탐색 | 하위 폴더까지 검색할지 여부 |
| 파일별 하위 디렉토리 | 측정 기준 폴더에서 각 역할을 찾을 위치. 빈칸/None은 기준 폴더 자체 |
| 확장자·접미어 | 파일 역할마다 따로 지정. 접미어를 제거한 공통 파일명으로 연결 |
| 고급 묶기 방식 | 같은 파일명 본체, 같은 폴더, 정규식으로 추출한 공통 키 |
| 역할별 키 규칙 | 파일 종류마다 접미사·하위 폴더가 달라도 같은 측정 키를 추출 |
| 제외 패턴 | 결과 폴더나 분석 대상이 아닌 파일을 제외 |
| 좌표·컬럼 설정 | 좌표 단위, 원점, 축 방향, 측정표 컬럼 이름과 순서 |

초기값은 다음 구조를 읽는다. 루트는 GUI에서 지정한다.

```text
<루트>/abc.dm3
<루트>/abc.tif
<루트>/result/abc.png
<루트>/result/abc_Result.xlsx
```

| 종류 | 하위 디렉토리 | 확장자 | 접미어 |
|---|---|---|---|
| dm3 | None | .dm3 | 빈 문자열 |
| image | None | .tif | 빈 문자열 |
| seg | result | .png | 빈 문자열 |
| table | result | .xlsx | _Result |

설정 JSON의 관련 부분은 다음과 같다. UI에서 이 표를 수정하면 같은 설정이 저장된다.

```json
{
  "match_mode": "layout",
  "recursive": true,
  "roles": {
    "dm3": {"directory": null, "extension": ".dm3", "suffix": ""},
    "image": {"directory": null, "extension": ".tif", "suffix": ""},
    "seg": {"directory": "result", "extension": ".png", "suffix": ""},
    "table": {"directory": "result", "extension": ".xlsx", "suffix": "_Result"}
  }
}
```

`lotA/abc`와 `lotB/abc`는 서로 다른 측정 묶음이다. 재귀 탐색은 각 측정 기준 폴더에서 같은 규칙을 적용하며, `result`만으로 서로 다른 측정의 파일을 섞지 않는다. `None`은 파일이 없다는 뜻이 아니라 하위 디렉토리가 없다는 뜻이다. 필요한 파일이 실제로 없으면 해당 역할 누락으로 표시한다.

설정을 바꾼 뒤 탐색 미리보기에서 각 묶음의 4종 파일과 상태를 확인한다. 후보가 여러 개이거나 필요한 파일이 빠진 묶음은 그대로 분석하지 않고 원인을 표시한다. 경로의 공백·한글도 사용할 수 있다. 기본 설정은 기존 입력처럼 컬러 CD 주석이 있는 세그멘테이션 PNG에 `seg_restore=true`를 적용한다. 순수 컬러 클래스 라벨 이미지라면 이 옵션을 꺼야 한다.

### 정규식 규칙을 설명할 때

예를 들어 이미지가 `sample17_raw.tif`, 측정표가 `sample17_measurements.xlsx`라면 두 파일에서 `sample17`을 공통 키로 추출하도록 역할별 규칙을 줄 수 있다. 폴더별로 같은 파일명이 반복되면 상대 경로의 상위 폴더명까지 키에 포함해야 한다.

```text
이미지 파일명 본체: (?P<key>.+)_raw$
측정표 파일명 본체: (?P<key>.+)_measurements$
```

실제 파일을 전달할 수 없다면 폴더·파일 이름의 **형태만 가상 예시로 설명**해도 된다. 그 구조에 맞는 규칙을 GUI에 넣고 미리보기에서 매칭 결과를 확인하면 된다. 규칙 저장 후에는 다음 실행에서 같은 설정을 불러와 탐색·분석한다.

## 3. 분석 기준

처음에는 전체 입력을 기준으로 분포를 살펴보는 탐색 모드로 시작할 수 있다. 이 모드의 점수는 현재 배치의 구성에 영향을 받는다. 정상으로 확인한 이미지가 있으면 정상 기준으로 선택한다.

기준 통계를 저장한 뒤 다른 배치에 재사용하면 동일한 기준으로 비교할 수 있다. 저장된 기준과 계산 파라미터가 맞는지, 새 카테고리에 기준이 있는지 확인한다. 정상 기준이 없는 카테고리를 자동으로 정상으로 해석하지 않는다.

`z`는 기준과 얼마나 다른지를 보는 진단값이다. 크기가 공정상 심각도를 뜻하지 않으며, 기본 상한 `30`에서는 포화된다. `NaN`이나 유효 피쳐 수가 적은 경우는 측정 불가·증거 부족으로 읽어야 한다. 이미지가 없다는 이유만으로 엣지 불량 점수를 부여하지 않는다.

## 4. 결과를 보며 개선 의견 전달하기

이미지 한 건마다 네 파일을 연결하며 D2에서는 이미지 → 카테고리 → 개별 CD 순서로 확인한다.
D4에서는 피쳐를 선택해 히스토그램과 산점도를 보고, 이미지별·카테고리별 분포를 같은 축에서 비교한다.
비교 대상을 좁힌 뒤 개별 CD 행이나 산점도 점을 선택하면 해당 이미지 위치를 강조한 오버레이를 확인할 수 있다.
히스토그램의 막대 하나에는 여러 측정이 들어가므로 개별 위치 확인에는 해당 범위의 CD 목록을 사용한다.

원본 데이터나 결과 파일을 전달하지 않아도 다음 내용을 화면에서 확인하며 설명할 수 있다.

| 관찰 | 함께 확인할 내용 |
|---|---|
| 파일이 예상보다 적게 잡힘 | 탐색 규칙, 제외 패턴, 공통 키, 누락·중복 후보 |
| 좌표가 경계에서 벗어남 | S/E 위치, 원점·축·단위, nm/px, 이미지 크기, 오프셋 |
| 한 CD만 이상하게 보임 | 같은 시퀀스 이웃, 원값과 z, 선택 CD의 명암·그래디언트 프로파일 |
| 카테고리 전체가 이상함 | 정상 기준 이미지, 기준 표본 수·중앙값·MAD, L2 시퀀스 요약 |
| 점수가 전부 비슷하거나 포화됨 | 분포, 결측률, 유효 피쳐 수, MAD 하한, 정상 기준 구성 |
| 세그멘테이션이 이상함 | 라벨 오버레이, 크기 불일치, 라벨 고유값, 경계 피쳐 |

예: “D2에서 S는 경계와 맞는데 E는 대부분 오른쪽에 있다”, “특정 카테고리의 delta_e가 같은 방향으로 치우친다”, “이전 설정보다 매칭 누락은 줄었지만 중복 후보가 늘었다”처럼 전달하면 된다. 이어지는 질문에 필요한 원값·기준 통계·설정·오류는 GUI에서 다시 찾아볼 수 있다.

GUI의 CSV·기준 통계 저장은 로컬에서 다시 확인하기 위한 기능이다. 결과나 메모가 자동으로 익명화되거나 외부로 전달되는 기능은 아니다.

## 5. 이번 버전에서 바로잡은 계산

- 이미지 미제공 상태의 엣지 증거는 미측정으로 유지한다. 실제 검증 실패와 구분해 점수·유효 피쳐 수에 반영한다.
- L2 `angle_median`의 정상 기준과 편차를 180도 주기로 계산한다. ±90도 경계의 작은 차이를 큰 회전으로 해석하지 않는다.
- 그룹 키가 결측이면 불명확한 인덱스 오류 대신 결측 컬럼과 건수를 포함한 입력 오류를 낸다. GUI에서는 입력 문제를 목록으로 확인한다.

실제 DM3 원본은 개발 환경에 없으므로 파일 포맷과 장비별 메타데이터 차이는 사내에서 확인해야 한다. 오류가 발생하면 화면의 단계·오류 코드·파일 역할과 메시지를 기준으로 입력 처리부터 조정한다. 합성 및 회귀 테스트 통과는 실제 TEM 오측정 탐지 성능을 보증하지 않는다.

## 6. 개발 검증 범위 (v0.6.0)

- 전체 테스트 194개 통과, 합성 감도·특이성 selftest 59개 통과.
- 실제 임시 TIFF·PNG·CSV·XLSX 파일과 한글 경로, 사용자 지정 폴더 구조의 매칭을 검증했다.
- DM3 축별 픽셀 크기 해석은 로더 응답을 대체한 테스트로 검증했다. 실제 장비 DM3 파일의 로딩은 미검증이다.
- GUI는 Streamlit AppTest로 데모·필터·정상 기준·설정 복원·값 선택 이벤트·원본 CD 연결을 검증했다. 실제 브라우저 마우스 조작과 화면 렌더링의 수동 검사는 개발 환경의 브라우저 연결 부재로 수행하지 못했다.
- Windows 실행 스크립트로 로컬 서버를 시작하고 상태 응답 `200 ok`를 확인했다. 패키지 wheel 빌드와 설치된 의존성 검사도 통과했다.
